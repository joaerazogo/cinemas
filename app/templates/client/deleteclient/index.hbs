<div class="center">
  <h1><strong><i>BUSCAR CLIENTE</i></strong></h1>
</div>
<br/>
<br/>
<br/>
<br/>
<form class="" action="" method="post">
  <div class="left col s1">
    <h6>Ingresar correo:</h6>
  </div>
  {{#if model.clientsRegistered}}
  {{else}}
    No existen correos actualmente
  {{/if}}
  {{#power-select
    options=model.clients
    searchField="email"
    selected=selectedClient
    allowClear=true
    noMatchesMessage="No se han encontrado coincidencias con este correo"
    onchange=(action "onChangeEmail")
    as |client|}}
      {{client.email}}
  {{/power-select}}

  <div class="center">
    <div class="row">
      <button class="btn waves-effect waves-light" type="submit" name="action" {{action "deleteEmail" selectedClient.email}}>Borrar
        <i class="material-icons right">send</i>
      </button>
      {{#link-to 'index'}}
        <button class="btn waves-effect waves-light" type="submit" name="action" >Atrás
          <i class="material-icons right">arrow_back</i>
        </button>
      {{/link-to}}

    </div>
    <div class="">
      {{#if model.showErrorMessage}}{{else}}Por favor ingrese un correo{{/if}}
    </div>
  </div>
</form>
{{outlet}}
