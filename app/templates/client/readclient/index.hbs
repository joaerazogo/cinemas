<div class="center">
  <h1><strong><i>LISTA DE CLIENTES EXISTENTES</i></strong></h1>
</div>
<form class="" action="" method="post">
  <div class="center">
    <h1>Bienvenido, por favor seleccione el correo del cliente que desea buscar</h1>
  </div>
  <div class="left col s1">
    <h6>Ingresar correo:</h6>
  </div>
  <div class="center">
    {{#if model.clientsRegistered}}
    {{else}}
      No existen correos actualmente
    {{/if}}
  </div>
  <div class="col s1">
    {{#power-select
      options=model.clients
      searchField="email"
      selected=selectedClient
      allowClear=true
      noMatchesMessage="No se han encontrado coincidencias con este correo"
      onchange=(action "onChangeEmail")
      as |client|}}
        {{client.email}}
    {{/power-select}}
  </div>
  <div class="center">
    <div class="row">
      {{#link-to 'client.readclient.readclients' selectedClient.email}}
        <button class="btn waves-effect waves-light" type="submit" name="action" {{action 'tryRead'}}>Leer
          <i class="material-icons right">arrow_forward</i>
        </button>
      {{/link-to}}
      {{#link-to 'index'}}
        <button class="btn waves-effect waves-light" type="submit" name="action" >Atrás
          <i class="material-icons right">arrow_back</i>
        </button>
      {{/link-to}}
      <div class="">
          {{#if model.showErrorMessage}}{{else}}Por favor ingrese un correo{{/if}}
      </div>
    </div>
  </div>
</form>
{{outlet}}
