<div class="center">
  <h1><strong><i>DATOS GENERALES DEL CLIENTE</i></strong></h1>
</div>
<div class="center">
  <h1>Actualice los datos del cliente</h1>
</div>
<div class="">
</div>
<div class="">
  <div class="row">
   <form class="col s12" >
     <div class="row">
       <div class="input-field col s6">
        <h6><span>Nombre: {{model.name}} </span></h6> {{input id=name value=name type="text" placeholder="enter name" required="true"}}
         <label for="name">Name:</label>
       </div>
     </div>
     {{#if fieldName}}
        El campo Nombre no puede ser vacío
     {{else}}
     {{/if}}
     <div class="row">
       <div class="input-field col s6">
         <h6><span>Apellido: {{model.lastName}}</span></h6> {{input id=last_name value=last_name type="text" placeholder="last name" required="true"}}
         <label for="name">Last Name:</label>
       </div>
     </div>
     {{#if fieldLastname}}
        El campo Apellido no puede ser vacío
     {{else}}
     {{/if}}
     <div class="row">
       <div class="input-field col s6">
         <h6><span>Dirección: {{model.address}}</span></h6> {{input id=address value=address type="text" placeholder="Address" required="true"}}
         <label for="direccion">Address:</label>
       </div>
     </div>
     {{#if fieldAddress}}
        El campo Dirección no puede ser vacío
     {{else}}
     {{/if}}
     <div class="row">
       <div class="input-field col s6">
         <h6><span>Teléfono: {{model.telephone}}</span></h6> {{input id=telephone value=telephone type="text" placeholder="telephone" required="true"}}
         <label for="name">Telephone:</label>
       </div>
     </div>
     {{#if fieldTelephone}}
        El campo Teléfono no puede ser vacío
     {{else}}
     {{/if}}
     {{#if messageTelephoneCorrectFormat}}
        El formato es incorrecto por favor digite de nuevo, deben ser números
     {{else}}
     {{/if}}

     <div class="row">
       <div class="input-field col s6">
         <h6><span>Correo electrónico: {{model.email}}</span></h6>{{input id=email value=email type="text" placeholder="email" required="true"}}
         <label for="email">Email</label>
       </div>
     </div>
     {{#if fieldEmail}}
        El campo correo electrónico no puede ser vacío
     {{else}}
     {{/if}}
     {{#if messageEmailCorrectFormat}}
        El formato es incorrecto por favor digite de nuevo, debe ser con @ y un dominio
     {{else}}
     {{/if}}

     <div class="row">
       <div class="input-field col s6">
         <h6><span>Contraseña: {{model.password}}</span></h6> {{input id=password value=password type="password" placeholder="password" required="true"}}
         <label for="password">Password</label>
       </div>
     </div>
     {{#if fieldPassword}}
        El campo Contraseña no puede ser vacío
     {{else}}
     {{/if}}
   </form>
 </div>
 <div class="center">
   <div class="row">
      <button class="btn waves-effect waves-light" type="submit" name="action" {{action 'updateClient' model.email}}>Actualizar
        <i class="material-icons right">send</i>
      </button>
    {{#link-to 'index'}}
      <button class="btn waves-effect waves-light" type="submit" name="action" {{action 'cleanMessage'}}>Cancelar
        <i class="material-icons right">arrow_back</i>
      </button>
    {{/link-to}}
   </div>
 </div>
</div>
{{outlet}}
