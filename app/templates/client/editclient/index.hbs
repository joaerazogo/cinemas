<form class="" action="" method="post">
  <div class="center">
    <h1>Please, select the email from client to searching</h1>
  </div>
  <div class="input-field col s6">
    <select class="browser-default" onchange={{action "editClient" value = "target.value"}}>
      <option value="" disabled selected>Choose your option</option>
          {{#each model as |client|}}
            <option value={{client}}>{{client.email}}</option>
          {{/each}}
    </select>
  </div>
  <div class="center">
    <div class="row">
      {{#Link-to 'index'}}
        <button class="btn waves-effect waves-light" type="submit" name="action">Cancelar
          <i class="material-icons right">arrow_back</i>
        </button>
      {{/Link-to}}
      {{#Link-to 'client.editclient.clienteedited' selectedClient}}
        <button class="btn waves-effect waves-light" type="submit" name="action">Leer
          <i class="material-icons right">arrow_forward</i>
        </button>
      {{/Link-to}}
    </div>
  </div>
</form>
{{outlet}}
